sudo: required

services:
  - docker

before_install:
  - sudo service mysql stop
  - docker run -d -p 3306:3306 -e MYSQL_ALLOW_EMPTY_PASSWORD=1 --name mysql mysql:5
  - sleep 10

language: node_js

node_js:
  - "6"
  - "8"
  - "10"
  - "12"
